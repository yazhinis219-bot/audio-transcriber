# Worker is now integrated into the gateway, no separate worker process needed
# This file is kept for compatibility but the worker functionality 
# is now handled within the gateway process

print("Worker functionality is now integrated into the gateway process.")
print("Please run 'python run_gateway.py' to start the complete system.")
